#include "main.h"
#include <string.h>

/**
 *_strlen - function returns the length of a string
 *@s: the string
 *Return: value og the length of string
*/

int _strlen(char *s)
{
int size;
size = strlen(s);
return (size);
}
