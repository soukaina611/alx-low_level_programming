#include "main.h"

/**
 * _islower - function checks lowercase character
 * @c:character to check
 * Return:1 if is lowercase else 0
*/

int _islower(int c)
{

if  (c >= 97 && c <= 122)
	return (1);
return (0);

}
