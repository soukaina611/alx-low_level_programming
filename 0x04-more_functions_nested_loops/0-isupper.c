#include "main.h"

/**
 *_isupper - function checks for uppercase character
 * @c: the number to check
 * Return: 1 for uppercase letter else 0
*/

int _isupper(int c)
{
for (c >= 65 ; c <= 90 ; c++)
{
return (1);
}
return (0);
}
